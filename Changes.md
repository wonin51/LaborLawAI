# Changes

## 2026-07-21

### 文档更新

- 新增本文件，用于集中记录项目后续变更。
- 更新 `README.md`，补充从项目根目录启动后端和前端的具体步骤。
- 明确后端默认地址为 `http://localhost:8080`，前端默认地址为 `http://localhost:5173`。
- 补充后端健康检查、前端安装依赖、开发启动、生产构建和测试命令。

### 当前运行入口

- 后端：`backend` 目录，使用 Maven 启动 Spring Boot 服务。
- 前端：`frontend` 目录，使用 npm 启动 Vite 开发服务。
